const initialState = {
  position: [0, 0],
};

const hooverReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
};

export default hooverReducer;
